org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true
# Jetifier rewrites old support-library dependencies to AndroidX. Nothing in
# this project needs it any more, and running it over every artifact is what
# ran the build out of heap once the current Firebase SDKs came in.
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
